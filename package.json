{
  "scripts": {
    "build": "bun run esbuild surfingkeys.js --bundle --outfile=surfingkeys.bundle.js --format=iife"
  },
  "dependencies": {
    "dayjs": "^1.11.18",
    "dotenv": "^17.2.1",
    "fuse.js": "^7.1.0",
    "turndown": "^7.2.0"
  },
  "devDependencies": {
    "esbuild": "^0.25.5"
  }
}
